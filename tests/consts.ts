export const DEFAULT_IMAGE_URL = new URL(
  "./resources/cats-dogs.png",
  import.meta.url,
).href;

export const DEFAULT_VIDEO_URL = new URL(
  "./resources/cats-dogs.webm",
  import.meta.url,
).href;

export const ZERO_SIZE_IMAGE_URL = new URL(
  "./resources/zero-size.svg",
  import.meta.url,
).href;

export const BROKEN_IMAGE_URL = new URL(
  "./resources/broken.png",
  import.meta.url,
).href;

export const BROKEN_VIDEO_URL = new URL(
  "./resources/broken.webm",
  import.meta.url,
).href;
